import React from "react";
import ReactDOM from "react-dom";

const hele = React.createElement("h1", null, "Hello");

const root = ReactDOM.createRoot(document.getElementById("fde"));

root.render(hele);
